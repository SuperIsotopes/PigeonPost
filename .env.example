# Only needed for local testing with `vercel dev`.
# In production, these are injected automatically once you connect an
# Upstash Redis database to your Vercel project (see README) -- you should
# not need to type these in by hand.

UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
